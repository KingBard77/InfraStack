# Calculate Source Snapshot

Reference-only full source copy from:

```text
templates/content/tools/aws/calculate-cost-aws/
```

This snapshot preserves the original `tool.html.twig`, `custom.css`, and `custom.js` used to baseline the calculate workspace sections.

Do not audit this as active source.

Final tools must not import these files at runtime. Use `templates/content/factory/phase-1/_family/calculate/workspace/` as the reusable section source.
