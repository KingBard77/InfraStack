family: scanning
version: 1.1.0
status: active
description: Baseline template for InfraStack scanner tools composed from shared `_base` workspace structure plus scanning-owned visual contracts, selected-item inspectors, reusable scanner model-core sources, evidence tables, export controls, and JSON restore boundaries.
agent_map: templates/content/factory/phase-1/_family/scanning/FAMILY.md

baseline_path: templates/content/factory/phase-1/_family/scanning/
preferred_baseline_path: templates/content/factory/phase-1/_family/scanning/
source_path: templates/content/factory/phase-1/_family/scanning/source/scan-web-security/
scaffold_path: templates/content/factory/phase-2/_content/scaffold/
content_sections_path: templates/content/factory/phase-2/_content/sections/

runtime_baseline:
    source_tool: templates/content/tools/security/scan-web-security/
    source_snapshot: templates/content/factory/phase-1/_family/scanning/source/scan-web-security/
    source_package: templates/content/factory/phase-1/_family/scanning/source/scan-web-security/
    model_core: templates/content/factory/phase-1/_family/scanning/source/scan-web-security/assets/bin/model-core.js
    copy_mode: base_plus_family_contract
    css_js_mode: section_owned
    model_core_sha256: 9846dafe6227ebb04bfc0021c826370fc747c371a6d2f264369d5dc599582073
    source_hashes:
        tool_html_twig: f137ca4be1d9f8fc322efb72e124c470a7aa0e98b762cdb231c8799a07349c0f
        custom_css: 19ecf2a79fa57519cc254d2849da0a7d94ec213bd8c23bc39fa37aff8fc7f3cd
        custom_js: 742cf7f9b24f4ba773a8f1b0fa9c01ddbbcc0978f80211b1d65d53ccd97b4e10
        model_core: 9846dafe6227ebb04bfc0021c826370fc747c371a6d2f264369d5dc599582073
    rule: Keep the full copied scanning source under source/scan-web-security/ as a reference-only full package snapshot. Audit it for freshness against production before using it as a seed. Use `_base/workspace` for common section shape and scanning workspace only for scanner-specific visual/model, selected-item, and model-core sources.

reference_tool:
    path: templates/content/tools/security/scan-web-security/
    purpose: Current working baseline reference for scanning-family composition. Use the workflow grammar and quality bar, not web-header-specific wording.
    aliases:
        - Web Security Scanner
        - Web security scanner
        - web-security-scanner
        - scan-web-security

runtime_parity:
    default_profile: web_security_scan
    profiles:
        web_security_scan:
            reference_tool: templates/content/tools/security/scan-web-security/
            css_reference_mode: production_reference
            min_css_reference_coverage: 0.90
            min_css_section_coverage: 0.90
            base_contract_sections:
                - family._base.workspace.00_shell
                - family._base.workspace.01_input-brief
                - family._base.workspace.02_basic-settings
                - family._base.workspace.03_custom-settings
                - family._base.workspace.05_result-summary
                - family._base.workspace.06_output-toolbar
                - family._base.workspace.07_table-output
                - family._base.workspace.08_json-restore
            family_sections:
                - family.scanning.workspace.04_visual-contract
                - family.scanning.workspace.04_selected-item
            required_behaviors:
                - hidden_first
                - primary_action_generates_output
                - custom_dropdown_state_sync
                - sort_state_sync
                - tab_state_sync
                - json_export_restore
            required_tabs:
                - Findings
                - Evidence
                - Surface
                - Notes
                - JSON
            required_actions:
                - Export PDF
                - Download CSV
                - Copy JSON
                - Download JSON
                - Import JSON
            allowed_differences:
                - tool_title
                - domain_labels
                - input_placeholder
                - scan_domain
                - finding_labels
                - evidence_copy

applies_to:
    - public web scanners
    - header posture scanners
    - cookie posture scanners
    - TLS posture scanners
    - redirect scanners
    - public endpoint scanners
    - DNS and record scanners
    - lightweight evidence scanners
    - server-assisted scan tools

section_adaptation:
    content:
        01_overview: scanner purpose, target boundary, evidence boundary, and trust boundary
        02_technical-details: request model, validation boundary, probes, findings, and payload shape
        03_example-prompts: Example Scan Inputs with paired Copy input controls unless the scanner is prompt-driven
        09_example-commands: literal scanner commands with paired Copy command controls when the scanner is command-oriented
        example_terminal_title: centered title case
        04_tips-prompts: scan input tips, scope tips, or evidence interpretation guidance
        05_how-to-use: target, options, scan, review, export, and restore workflow
        06_export-notes: PDF or report, CSV, JSON, copy, and import restore boundaries
        07_faq: scanner scope boundaries and evidence limitations
        08_acronyms: optional abbreviation expansion table; scanner, protocol, and domain acronyms
        11_glossary: optional broader terminology table; broader domain, command, workflow, platform, or estimate terminology
        10_references: complete factual content delivery, valid source-backed citations, and References rows when final scanning content cites sources
    workspace:
        _base.00_shell: shared workspace frame, panel rhythm, and responsive shell
        _base.01_input-brief: shared target input and scan action frame
        _base.02_basic-settings: shared request method, redirects, timeout, validation, client profile, and companion-probe frame
        _base.03_custom-settings: shared optional Custom settings panel frame
        04_visual-contract: optional scan visual and model contract for posture rings, severity metrics, evidence cards, finding rows, and coverage tones
        04_selected-item: optional selected finding/evidence inspector; no-op unless the scanner supports selection
        _base.05_result-summary: shared hidden-first result summary, score/status, metrics, chips, and target details frame
        _base.06_output-toolbar: shared ID-first sort, row copy, CSV/report/JSON, optional import, and output actions frame
        _base.07_table-output: shared findings, evidence tables, surface details, result tabs, and JSON shell
        _base.08_json-restore: shared scanner JSON payload output and restore frame

workspace_section_file_contract:
    required_per_section:
        - README.md
        - demo.html
        - page.html.twig
        - section.css
        - section.js
    role: Scanning-owned workspace sections match the architecture family bundle shape. Common sections are sourced from `_base/workspace`.
    visual_contract_additional_files:
        - manifest.yml
        - model-core.js

required_output_files:
    - card.yml
    - content.md
    - custom.css
    - custom.js
    - meta.yml
    - tool.html.twig

optional_output_files:
    - assets/bin/model-core.js
    - assets/icon/
    - assets/img/post.html.twig
    - src/Controller/Tools/<Category>/<Tool>/CustomController.php

required_first_line_markers:
    content.md: "[//]: # (content.md)"
    card.yml: "# card.yml"
    custom.css: "/* custom.css */"
    custom.js: "// custom.js"
    meta.yml: "# meta.yml"
    tool.html.twig: "{# tool.html.twig #}"

workspace_flow:
    - target_input
    - scan_options
    - visual_contract_when_needed
    - hidden_first_result_empty_state
    - result_summary
    - output_toolbar_sort_copy_export_import
    - result_tabs_evidence_tables
    - json_payload_output_restore

source_namespaces:
    - family._base.workspace.00_shell
    - family._base.workspace.01_input-brief
    - family._base.workspace.02_basic-settings
    - family._base.workspace.03_custom-settings
    - family.scanning.workspace.04_visual-contract
    - family.scanning.workspace.04_selected-item
    - family._base.workspace.05_result-summary
    - family._base.workspace.06_output-toolbar
    - family._base.workspace.07_table-output
    - family._base.workspace.08_json-restore

export_priority:
    - PDF_or_print_report
    - CSV
    - JSON
    - Import_JSON_restore_when_implemented

generation_rules:
    - Use this family template as the baseline for scanning tools.
    - Use scan as the family verb for new scanning tool identities.
    - Use scan-prefixed names for scanning family outputs.
    - Generate the final runtime package under templates/content/tools/<category>/<tool-slug>/.
    - Do not use the family directory as the final runtime package.
    - Do not omit tool-local files because the family source exists.
    - Keep the full copied scanning source under source/scan-web-security/ as the approved production package snapshot.
    - Audit the source snapshot for freshness against templates/content/tools/security/scan-web-security/ before using it as a seed; do not import it directly as active workspace section source.
    - Compose scanning output from `source_namespaces`, using `_base/workspace` namespace markers for common shell, input, settings, summary, toolbar, table, and JSON restore contracts.
    - Keep reusable scanning-owned runtime behavior in namespace-marked source package runtime files.
    - Keep source package assets/bin/model-core.js as the reusable model-core source when the source tool ships one.
    - Use the source package 04_visual-contract namespace for scanning tools that need reusable visual scan state beyond plain target input and evidence tables.
    - Omit 04_visual-contract for pure form-and-table scanners only when the omission is recorded as accepted divergence.
    - Keep selected finding, evidence detail, and runtime inspector behavior out of 04_visual-contract unless a dedicated inspector section is deliberately added.
    - Use Web Security Scanner as the current working baseline reference for scanning source grammar.
    - Mirror the stabilized scanning output shell by adapting `_base` hidden-first result summary, toolbar shell, tabs shell, table shell, and JSON panel.
    - Keep the compact circular score ring with centered score and denominator text by adapting `_base/workspace/05_result-summary` and scanner visual-contract state when used.
    - Keep custom preference controls inside the scanner shell without nested card framing unless a separate inspector section is justified.
    - Keep summary, toolbar, tabs, and table shells visually separated without unintended divider lines between them by adapting `_base` summary, toolbar, and table-output sources.
    - Default sortable finding output to ID when stable row order exists.
    - Keep result table copy columns sticky or frozen when horizontal scrolling is needed.
    - Keep output action buttons compact and boxed, not oversized pills.
    - Keep Basic/Custom dropdowns as width-contained native selects with visible browser-owned popups; output toolbar menus may keep their dedicated toolbar contract.
    - Use Example Scan Inputs with paired Copy input controls for form-first scanners.
    - Keep generic markdown copy buttons out of those paired example controls.
    - Do not copy web-header-specific language into unrelated scanners.
    - Apply category/provider token lineage, restrained visual weight, and tool-specific namespace.
    - Keep CSS and JavaScript scoped to the final tool.
    - Keep content domain-native and aligned with actual behavior.
    - Use the tokenized family source package from templates/content/factory/phase-1/_family/scanning/source/scan-web-security/.
    - Use shared content section source from templates/content/factory/phase-2/_content/sections/.
    - Apply phase-2 content sections as a full contract: content.md markup, section.css visual behavior, and section.js helper behavior when present.
    - Final tool custom.css and custom.js must preserve the applied section card frame, heading divider, typography, table, list, details, FAQ, copy, citation, prompt, or command behavior unless a documented shared include exists.
    - Use 10_references for complete factual content delivery and when final content includes valid source-backed citations, with in-text citation links targeting matching References row IDs.
    - Do not create a family-local sections/ directory.

validation_rules:
    - README.md exists.
    - manifest.yml exists.
    - source package folder exists.
    - source demo.html exists.
    - Shared phase-2 content section source exists under templates/content/factory/phase-2/_content/sections/.
    - Applied shared content sections are validated in the final tool package for namespace markers, unresolved placeholders, required custom.css selectors, and helper hooks when used.
    - Citation links and References rows are paired when 10_references is used.
    - Scanning root family manifest declares `source_namespaces`.
    - Scanning source namespace markers exist inside the source package runtime files.
    - Scanning-owned source namespace markers exist inside the source package runtime files.
    - source package assets/bin/model-core.js exists when the reference tool ships one.
    - source/ contains only source/scan-web-security/.
    - source/scan-web-security/card.yml, content.md, custom.css, custom.js, demo.html, meta.yml, tool.html.twig, assets/bin/model-core.js, assets/icon/, and assets/img/post.html.twig exist.
    - source/scan-web-security/ matches templates/content/tools/security/scan-web-security/ file-for-file, except local Finder artifacts.
    - root-level loose source/custom.css and source/custom.js do not exist.
    - source package assets/bin/model-core.js exists and matches the declared reusable model-core hash.
    - No duplicated scanning sections/ directory exists under templates/content/factory/phase-1/_family/scanning/.
    - Required final output files are created in the final tool package.
    - Required first-line markers are present.
    - Tool namespace is unique.
    - CSS is scoped under the tool root.
    - JavaScript DOM references match Twig IDs.
    - Scan query, result summary, tables, exports, and JSON use one normalized model or payload.
    - Export controls match implemented exports.
    - Import controls appear only when JSON restore is implemented and validated.
    - Result table is hidden on first refresh unless validated restore intentionally loads output.
    - Example Scan Input copy controls target the matching input block without duplicate generic copy buttons.
    - Content does not claim compliance, security certification, exploitability, production readiness, or full coverage unless validated.
