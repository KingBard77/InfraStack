# Assessment Source Snapshot

Reference-only full source copy from:

```text
templates/content/tools/cis/assess-ubuntu-2204-cis/
```

This snapshot preserves the original `tool.html.twig`, `custom.css`, `custom.js`, and `assets/custom.json.twig` used to baseline the assessment workspace sections.

Do not audit this as active source.

Final tools must not import these files at runtime. Use `templates/content/factory/phase-1/_family/assessment/workspace/` as the reusable section source.
