# InfraStack Factory Phase 2

`phase-2/` owns final package and support content delivery.

Use it after the phase-1 tool behavior contract is clear.

```text
_content/
├── scaffold/
└── sections/
```

`scaffold/` provides the reusable package shell. `sections/` provides reusable support content sections from `01_overview` through `11_glossary`.
