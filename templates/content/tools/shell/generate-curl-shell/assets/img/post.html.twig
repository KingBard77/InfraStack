{% set visualVariant = variant|default('card') %}
{% set showPostDetail = visualVariant in ['hero', 'hero-small'] %}
{% if visualVariant == 'style' %}
<style>
.generate-curl-shell-post-visual {
    --post-start: #0f766e;
    --post-end: #14b8a6;
    --post-accent: #ccfbf1;
}

.generate-curl-shell-post-visual .tool-post-node:nth-child(1),
.generate-curl-shell-post-visual .tool-post-node:nth-child(3),
.generate-curl-shell-post-visual .tool-post-node:nth-child(5) {
    background: rgba(204, 251, 241, 0.24);
}

.generate-curl-shell-post-visual .tool-post-icon {
    background: rgba(20, 184, 166, 0.28);
}
</style>
{% else %}
<div
  class="tool-post-visual generate-curl-shell-post-visual tool-post-visual-{{ visualVariant }}"
  style="--post-start: {{ post.card_gradient_start|default('#0f766e') }}; --post-end: {{ post.card_gradient_end|default('#14b8a6') }};"
>
  <div class="tool-post-bg-icons" aria-hidden="true">
    <i class="bi bi-terminal"></i>
    <i class="bi bi-globe2"></i>
    <i class="bi bi-braces"></i>
    <i class="bi bi-key"></i>
    <i class="bi bi-filetype-json"></i>
  </div>
  <div class="tool-post-header">
    <span class="tool-post-kicker">{{ post.card_kicker|default(post.category|default('Shell')) }}</span>
    <span class="tool-post-icon"><i class="{{ post.card_icon_class|default('bi bi-terminal') }}"></i></span>
  </div>
  <div class="tool-post-title">{{ post.card_image_title|default(post.title|default('cURL Command Generator')) }}</div>
  <div class="tool-post-map" aria-hidden="true">
    <span class="tool-post-node"><i class="bi bi-terminal"></i></span>
    <span class="tool-post-node"></span>
    <span class="tool-post-node"><i class="bi bi-globe2"></i></span>
    <span class="tool-post-node"></span>
    <span class="tool-post-node"><i class="bi bi-filetype-json"></i></span>
  </div>
  <div class="tool-post-footer">
    <span>{{ post.reading_time|default('7 min read') }}</span>
    <span>{{ post.category|default('Shell') }}</span>
  </div>
  {% if showPostDetail %}
    <div class="tool-post-detail">
      <div class="tool-post-meta">
        <span><i class="bi bi-chevron-down"></i> {{ post.date_display|default('Apr. 2nd, 2026') }}</span>
        <span><i class="bi bi-three-dots-vertical"></i> {{ post.category|default('Shell') }}</span>
        <span><i class="bi bi-clock"></i> {{ post.reading_time|default('7 min read') }}</span>
      </div>
      <div class="tool-post-detail-title">{{ post.title|default('cURL Command Generator') }}</div>
      <span class="tool-post-read-more">
        Read More
        <i class="bi bi-arrow-right"></i>
      </span>
    </div>
  {% endif %}
</div>
{% endif %}
