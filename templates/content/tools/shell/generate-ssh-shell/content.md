[//]: # (content.md)

## Workspace

This package implements the SSH input brief, Basic settings, Custom settings, shell visual-contract preview, result summary, output toolbar, operation table, warning rows, and JSON restore workflow. It does not execute SSH, test a host, validate credentials, or claim remote host readiness.

Use the workspace as a command-building and review surface. Review the generated command manually before using SSH on a real system.
