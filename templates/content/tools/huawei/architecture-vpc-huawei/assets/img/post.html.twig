{% set visualVariant = variant|default('card') %}
{% set showPostDetail = visualVariant in ['hero', 'hero-small'] %}
{% if visualVariant == 'style' %}
<style>
.architecture-vpc-huawei-post-visual,
.architecture-vpc-huawei-post-visual * {
    box-sizing: border-box;
}

.architecture-vpc-huawei-post-visual {
    --post-start: #C7000B;
    --post-end: #111827;
    --post-ink: #ffffff;
    --post-muted: rgba(255, 255, 255, 0.76);
    aspect-ratio: 16 / 10;
    background:
        linear-gradient(rgba(255, 255, 255, 0.11) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, 0.11) 1px, transparent 1px),
        linear-gradient(135deg, var(--post-start), var(--post-end));
    background-size: 28px 28px, 28px 28px, auto;
    border-radius: inherit;
    color: var(--post-ink);
    display: grid;
    gap: 18px;
    min-height: 220px;
    overflow: hidden;
    padding: 22px 24px;
    position: relative;
}

.architecture-vpc-huawei-post-visual::after {
    background: linear-gradient(180deg, rgba(15, 23, 42, 0.04), rgba(15, 23, 42, 0.44));
    content: "";
    inset: 0;
    position: absolute;
}

.architecture-vpc-huawei-post-header,
.architecture-vpc-huawei-post-stage,
.architecture-vpc-huawei-post-footer,
.architecture-vpc-huawei-post-detail {
    position: relative;
    z-index: 1;
}

.architecture-vpc-huawei-post-header {
    align-items: flex-start;
    display: flex;
    gap: 16px;
    justify-content: space-between;
}

.architecture-vpc-huawei-post-kicker {
    color: var(--post-muted);
    font-size: 0.76rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.architecture-vpc-huawei-post-title {
    color: #ffffff;
    font-size: clamp(1.25rem, 2.4vw, 2.05rem);
    font-weight: 900;
    line-height: 1.08;
    margin-top: 10px;
    max-width: 80%;
}

.architecture-vpc-huawei-post-icon {
    align-items: center;
    background: rgba(255, 255, 255, 0.16);
    border: 1px solid rgba(255, 255, 255, 0.32);
    border-radius: 12px;
    display: inline-flex;
    height: 46px;
    justify-content: center;
    width: 46px;
}

.architecture-vpc-huawei-post-icon i {
    color: #ffffff;
    font-size: 1.45rem;
}

.architecture-vpc-huawei-post-stage {
    display: grid;
    gap: 10px;
    grid-template-columns: 0.9fr 1fr 1fr 1fr;
    margin-top: 8px;
    max-width: 590px;
}

.architecture-vpc-huawei-post-node,
.architecture-vpc-huawei-post-line {
    min-height: 42px;
}

.architecture-vpc-huawei-post-node {
    align-items: center;
    background: rgba(255, 255, 255, 0.16);
    border: 1px solid rgba(255, 255, 255, 0.28);
    border-radius: 10px;
    display: flex;
    font-size: 0.76rem;
    font-weight: 800;
    justify-content: center;
    padding: 8px;
    text-align: center;
}

.architecture-vpc-huawei-post-line {
    align-items: center;
    display: flex;
}

.architecture-vpc-huawei-post-line::before {
    background: rgba(255, 255, 255, 0.62);
    content: "";
    display: block;
    height: 3px;
    width: 100%;
}

.architecture-vpc-huawei-post-footer {
    align-items: center;
    color: var(--post-muted);
    display: flex;
    flex-wrap: wrap;
    font-size: 0.82rem;
    font-weight: 800;
    gap: 10px;
}

.architecture-vpc-huawei-post-footer span + span::before {
    content: "|";
    margin-right: 10px;
}

.architecture-vpc-huawei-post-detail {
    background: rgba(15, 23, 42, 0.34);
    border: 1px solid rgba(255, 255, 255, 0.22);
    border-radius: 10px;
    display: grid;
    gap: 8px;
    margin-top: auto;
    padding: 12px;
}

.architecture-vpc-huawei-post-detail-title {
    color: #ffffff;
    font-size: 1rem;
    font-weight: 900;
}

.architecture-vpc-huawei-post-meta {
    color: var(--post-muted);
    display: flex;
    flex-wrap: wrap;
    font-size: 0.74rem;
    font-weight: 800;
    gap: 10px;
}
</style>
{% else %}
<div
  class="architecture-vpc-huawei-post-visual architecture-vpc-huawei-post-visual-{{ visualVariant }}"
  style="--post-start: {{ post.card_gradient_start|default('#C7000B') }}; --post-end: {{ post.card_gradient_end|default('#111827') }};"
>
  <div class="architecture-vpc-huawei-post-header">
    <div>
      <span class="architecture-vpc-huawei-post-kicker">{{ post.card_kicker|default(post.category|default('Huawei Cloud')) }}</span>
      <div class="architecture-vpc-huawei-post-title">{{ post.card_image_title|default(post.title|default('Huawei Cloud VPC Architecture')) }}</div>
    </div>
    <span class="architecture-vpc-huawei-post-icon"><i class="{{ post.card_icon_class|default('bi bi-cloud') }}"></i></span>
  </div>
  <div class="architecture-vpc-huawei-post-stage" aria-hidden="true">
    <span class="architecture-vpc-huawei-post-node">WAF</span>
    <span class="architecture-vpc-huawei-post-line"></span>
    <span class="architecture-vpc-huawei-post-node">VPC</span>
    <span class="architecture-vpc-huawei-post-node">Subnets</span>
  </div>
  <div class="architecture-vpc-huawei-post-footer">
    <span>Zones</span>
    <span>Elastic Load Balance</span>
    <span>Enterprise Router</span>
    <span>Flow CTS</span>
  </div>
  {% if showPostDetail %}
    <div class="architecture-vpc-huawei-post-detail">
      <div class="architecture-vpc-huawei-post-meta">
        <span>{{ post.date_display|default('May. 13th, 2026') }}</span>
        <span>{{ post.category|default('Huawei Cloud') }}</span>
        <span>{{ post.reading_time|default('4 min read') }}</span>
      </div>
      <div class="architecture-vpc-huawei-post-detail-title">{{ post.title|default('Huawei Cloud VPC Architecture') }}</div>
    </div>
  {% endif %}
</div>
{% endif %}
