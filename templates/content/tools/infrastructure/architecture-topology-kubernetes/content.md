[//]: # (content.md)

## Draft Workspace Slice

This draft package currently implements the Kubernetes topology input brief, Basic settings, Custom settings, and architecture visual-contract preview. It does not generate the full architecture canvas, export files, restore JSON, or provide full support content yet.

Use the preview to shape the topology brief before the full architecture workspace is built.
